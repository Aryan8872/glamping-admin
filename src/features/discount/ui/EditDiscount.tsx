import { Discount } from "../types/discountTypes";

export default function EditDiscount({
  discount,
  onClose,
}: {
  discount: Discount;
  onClose: () => void;
}) {
  return <div className="">edit discount</div>;
}
